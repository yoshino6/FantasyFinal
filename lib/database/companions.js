const initializeCompanions = async (pool) => {
    await pool.query(`CREATE TABLE IF NOT EXISTS player_companions (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,character_id BIGINT UNSIGNED NOT NULL,template_code VARCHAR(64) NOT NULL,name VARCHAR(64) NOT NULL,level INT NOT NULL DEFAULT 1,experience BIGINT NOT NULL DEFAULT 0,source_level INT NOT NULL DEFAULT 1,source_spawn_id BIGINT UNSIGNED NULL,is_out TINYINT NOT NULL DEFAULT 0,intimacy DECIMAL(6,2) NOT NULL DEFAULT 0,stability INT NOT NULL DEFAULT 80,injured TINYINT NOT NULL DEFAULT 0,specialty VARCHAR(32) NOT NULL DEFAULT 'watch',released_at DATETIME NULL,created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,KEY idx_companion_owner(character_id,released_at),UNIQUE KEY uk_companion_source(source_spawn_id),CONSTRAINT fk_companion_character FOREIGN KEY(character_id) REFERENCES characters(id) ON DELETE CASCADE) ENGINE=InnoDB`);
    await pool.query(`CREATE TABLE IF NOT EXISTS companion_invitations (character_id BIGINT UNSIGNED NOT NULL PRIMARY KEY,spawn_id BIGINT UNSIGNED NOT NULL UNIQUE,template_code VARCHAR(64) NOT NULL,name VARCHAR(64) NOT NULL,source_level INT NOT NULL,created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,CONSTRAINT fk_companion_invite_character FOREIGN KEY(character_id) REFERENCES characters(id) ON DELETE CASCADE) ENGINE=InnoDB`);
    await pool.query(`CREATE TABLE IF NOT EXISTS companion_daily (companion_id BIGINT UNSIGNED NOT NULL,day_key DATE NOT NULL,feed_count INT NOT NULL DEFAULT 0,adventure_count INT NOT NULL DEFAULT 0,find_count INT NOT NULL DEFAULT 0,bread_used TINYINT NOT NULL DEFAULT 0,PRIMARY KEY(companion_id,day_key),CONSTRAINT fk_companion_daily FOREIGN KEY(companion_id) REFERENCES player_companions(id) ON DELETE CASCADE) ENGINE=InnoDB`);
    await pool.query(`CREATE TABLE IF NOT EXISTS companion_combat_progress (session_id CHAR(36) NOT NULL,character_id BIGINT UNSIGNED NOT NULL,companion_id BIGINT UNSIGNED NOT NULL,actions INT NOT NULL DEFAULT 0,last_action_key VARCHAR(128) NOT NULL DEFAULT '',healed TINYINT NOT NULL DEFAULT 0,injured TINYINT NOT NULL DEFAULT 0,PRIMARY KEY(session_id,character_id),CONSTRAINT fk_companion_combat_owner FOREIGN KEY(character_id) REFERENCES characters(id) ON DELETE CASCADE) ENGINE=InnoDB`);
};

export { initializeCompanions };
