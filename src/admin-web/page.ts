import type { WebSession } from '../game/admin-web.service';

export const adminCoverPath = '/admin/assets/pear-admin-cover.png';

const pageShell = (title: string, body: string, script: string, nonce: string) => `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>${title}</title><style>
:root{color-scheme:light;font-family:Inter,"Microsoft YaHei",sans-serif;background:#eef9ff;color:#18314a}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:#eef9ff}button,input,select,textarea{font:inherit}button{border:0;border-radius:10px;padding:11px 15px;background:#168fb7;color:#fff;cursor:pointer;font-weight:700}button:hover{filter:brightness(1.08)}button.secondary{background:#e5f2f7;color:#28566d}button.link{color:#087da7;background:none;padding:0;border-radius:0}input,select,textarea{width:100%;padding:12px;border-radius:10px;border:1px solid #a9d2e3;background:#fff;color:#17334e}textarea{min-height:100px;resize:vertical}.login{min-height:100vh;display:grid;grid-template-columns:minmax(0,1fr) minmax(380px,46%);background:url("${adminCoverPath}") center/cover no-repeat}.intro{padding:10vh 9vw;align-self:center;max-width:680px}.eyebrow{letter-spacing:.18em;color:#14779b;font-size:12px}.intro h1{font-size:clamp(42px,6vw,78px);margin:.25em 0}.intro p,.muted,.who{color:#506b80;line-height:1.7}.login-panel{display:flex;align-items:center;justify-content:center;padding:30px;background:rgba(241,250,255,.20);backdrop-filter:blur(16px)}.card{width:min(100%,400px);background:#ffffffdf;border:1px solid #b5dcea;border-radius:20px;padding:32px;box-shadow:0 24px 80px #49789945}.card h2{margin:0 0 8px}.field{display:block;margin:18px 0 0}.field span{display:block;font-size:13px;color:#45667a;margin:0 0 7px}.error{min-height:20px;color:#a5263b;font-size:13px;margin-top:14px}.app{display:grid;grid-template-columns:250px 1fr;min-height:100vh}.side{padding:28px 16px;background:#f8fdff;border-right:1px solid #c6e4ee}.brand{padding:0 12px 24px;font-size:22px;font-weight:800}.brand small{display:block;font-size:11px;color:#2c86a5;letter-spacing:.12em;margin-top:5px}.nav{width:100%;text-align:left;background:transparent;color:#31556e;margin:4px 0}.nav.active,.nav:hover{background:#d9f2fa}.main{padding:34px;max-width:1500px;width:100%;margin:0 auto}.top{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:26px}.top h1{margin:0;font-size:28px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:14px}.metric,.panel{background:#fff;border:1px solid #c8e4ed;border-radius:14px;padding:18px;box-shadow:0 8px 24px #45738d12}.metric strong{font-size:32px;display:block;margin-top:8px}.panel{margin-top:16px}.panel h2,.panel h3{margin-top:0}.toolbar,.actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.toolbar input,.toolbar select{width:auto;min-width:150px;flex:1}.toolbar select{flex:0 1 160px}table{border-collapse:collapse;width:100%;font-size:14px}th,td{text-align:left;padding:11px;border-bottom:1px solid #d6eaf0;vertical-align:top}th{color:#18799c;font-size:12px;letter-spacing:.04em}.tag{display:inline-block;padding:3px 8px;border-radius:999px;background:#dceff6;color:#24536c;font-size:12px}.tag.critical{background:#ffe0e4;color:#8d2638}.tag.warning{background:#fff0c9;color:#79520d}.detail,.two{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px}.list{margin:0;padding-left:18px}.list li{margin:7px 0;color:#405e71}.empty{color:#6a8494;padding:18px 0}@media(max-width:800px){.login{grid-template-columns:1fr}.intro{min-height:35vh;padding:48px 32px}.login-panel{padding:32px}.app{grid-template-columns:1fr}.side{border-right:0;border-bottom:1px solid #c6e4ee}.nav{display:inline-block;width:auto}.main{padding:20px}.toolbar input,.toolbar select{min-width:100%;flex-basis:100%}}
.login-panel{background:rgba(241,250,255,.25);backdrop-filter:none}
.card{background:rgba(255,255,255,.30)}
.intro{width:100%;max-width:none;display:flex;flex-direction:column;align-items:center;text-align:center}
.intro h1{width:max-content;margin:.25em auto;white-space:nowrap}
.login-panel input{background:rgba(255,255,255,.60)}
.login-panel input[name="username"],.login-panel input[name="password"]{background:rgba(255,255,255,.60)}
.login-panel button{background:rgba(22,143,183,.60)}
</style></head><body>${body}<script nonce="${nonce}">${script}</script></body></html>`;

const adminRoleLabel = (role: WebSession['role']) => ({ owner: '主人', admin: '管理员', viewer: '只读观察员' })[role];

const clientScript = (role: WebSession['role']) => String.raw`
const csrf=(document.cookie.split('; ').find(item=>item.startsWith('fantasyfinal_admin_csrf='))||'').split('=').slice(1).join('');
const role=${JSON.stringify(role)};
const labels={role:{owner:'主人',admin:'管理员',viewer:'只读观察员'},activity:{active:'行动中',resting:'休息中',unconscious:'昏迷中',detained:'被关押'},playerStatus:{active:'正常',inactive:'未激活',disabled:'已禁用',banned:'已封禁',deleted:'已注销'},profession:{warrior:'战士',knight:'骑士',swordsman:'剑士',ranger:'游侠',archer:'弓箭手',rogue:'盗贼',mage:'法师',wizard:'法师',priest:'祭司',cleric:'牧师'},secondaryProfession:{alchemist:'炼金师',deconstructor:'解构师'},equipment:{weapon:'武器',main_hand:'主手武器',off_hand:'副手装备',head:'头部',chest:'胸部',legs:'腿部',feet:'足部',ring:'戒指',amulet:'护符'},playerEvent:{'character.created':'角色创建','social.blessing.claimed':'祝福已领取','evolution.seed_awakened':'进化之种感悟','evolution.injected':'注射进化药剂','evolution.mutation_paused':'变异已暂停','evolution.mutation_resumed':'变异已恢复','evolution.mutation_stabilized':'变异已稳定','evolution.mutation_archived':'变异已封存','home.purchased':'购置家园','home.left':'离开家园','home.renamed':'家园改名'},setting:{experience_multiplier:'经验倍率',drop_multiplier:'掉落倍率',copper_multiplier:'铜币倍率'},action:{'web.login':'网页登录','web.account.create':'创建管理员账号','web.account.enable':'启用管理员账号','web.account.disable':'禁用管理员账号','world.multiplier.update':'修改世界倍率','player.audit':'玩家数据核查'},target:{character:'玩家角色',global_setting:'全局设置',admin_account:'管理员账号'},risk:{low:'低风险',medium:'中风险',high:'高风险'},operationStatus:{committed:'已完成',failed:'执行失败',rolled_back:'已回滚'},severity:{warning:'警告',critical:'严重'},rule:{scheduled_job_repeated_failure:'定时任务连续失败',stale_player_travel:'玩家行程超时',database_slow:'数据库响应缓慢'},job:{'system.startup.initialize':'启动初始化','travel.settlement':'行程结算','combat.timeout_sweep':'战斗超时清理','world.dynamic_settlement':'动态世界结算','monitor.evaluate':'监控检查'},jobStatus:{success:'成功',failed:'失败'}};
Object.assign(labels,{action:{...labels.action,'mail.send':'发放游戏邮件'},target:{...labels.target,mail_delivery:'邮件发放'},encounterStatus:{active:'进行中',resolved:'已解决',expired:'已过期',cancelled:'已取消'},patrolStatus:{responding:'赶往奇遇',stationed:'常驻',patrolling:'巡游中'},worldEvent:{'scene.opened':'公共奇遇开启','npc.presence':'巡游实体状态变更'},worldOutcome:{active:'进行中',deployed:'已派遣',returned:'已返程',stationed:'已常驻'}});
const translated=(group,value,fallback='未知')=>labels[group]?.[String(value)]??fallback;
const targetText=target=>{if(!target)return '—';if(target.kind==='global_setting')return translated('setting',target.id,'世界参数');if(target.kind==='character')return '玩家角色：'+target.id;if(target.kind==='admin_account')return '管理员账号：'+target.id;return '操作目标';};
const content=document.getElementById('content');
const title=document.getElementById('title');
const escape=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const date=value=>value?new Date(value).toLocaleString('zh-CN',{hour12:false}):'—';
const metric=(name,value)=>'<div class="metric"><span class="muted">'+escape(name)+'</span><strong>'+escape(value)+'</strong></div>';
const position=value=>value?value.x+'，'+value.y+'，'+value.z:'位置暂未同步';
const request=async(path,options={})=>{const headers=Object.assign({},options.headers||{});if(options.method&&options.method!=='GET')headers['X-FF-CSRF']=csrf;const response=await fetch(path,Object.assign({},options,{headers}));const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.message||'请求失败。');return body};
const showError=error=>{content.innerHTML='<div class="panel"><h2>无法完成请求</h2><p class="muted">'+escape(error.message||error)+'</p></div>'};
const playerQuery=()=>{const params=new URLSearchParams();[['keyword','player-search'],['region','player-region'],['activity','player-activity'],['status','player-status']].forEach(([key,id])=>{const value=document.getElementById(id)?.value?.trim();if(value)params.set(key,value)});return params.toString()};

async function dashboard(){title.textContent='总览';const data=await request('/api/admin/dashboard');const game=data.system.game||{};content.innerHTML='<div class="grid">'+metric('注册角色',data.players)+metric('24 小时管理操作',data.operationsToday)+metric('未恢复告警',data.openAlerts)+metric('活跃战斗',game.battles??'—')+metric('进行中旅行',game.travelling??'—')+'</div><div class="panel"><h2>服务状态</h2><p class="muted">服务运行 '+escape(Math.floor(data.system.serviceUptimeSeconds/60))+' 分钟 · 数据库 '+(data.system.game?'可用':'暂不可用')+'</p></div>'}

async function players(){title.textContent='玩家';content.innerHTML='<div class="panel"><div class="toolbar"><input id="player-search" placeholder="角色名、QQ 号或昵称"><input id="player-region" placeholder="区域名称或代号（可选）"><input id="player-activity" placeholder="活动状态（可选）"><input id="player-status" placeholder="账户状态（可选）"><button id="search-players">筛选</button></div><div id="player-table" class="empty">正在查询…</div></div>';const load=async()=>{const data=await request('/api/admin/players?'+playerQuery());document.getElementById('player-table').innerHTML=data.entries.length?'<table><thead><tr><th>角色</th><th>等级</th><th>位置</th><th>状态</th><th>资产</th><th>更新时间</th></tr></thead><tbody>'+data.entries.map(row=>'<tr><td><button class="link" data-player="'+row.characterId+'">'+escape(row.name)+'</button><br><span class="muted">'+escape(row.qqUserId)+'</span></td><td>等级 '+row.level+' · 阶段 '+row.realmStage+'</td><td>'+escape(row.region.name)+'</td><td><span class="tag">'+escape(translated('activity',row.activity,'未知状态'))+'</span></td><td>'+row.copper+' 铜币</td><td>'+date(row.updatedAt)+'</td></tr>').join('')+'</tbody></table>':'<div class="empty">没有符合条件的玩家。</div>'};document.getElementById('search-players').onclick=()=>load().catch(showError);await load()}

async function player(id){title.textContent='玩家详情';const data=await request('/api/admin/players/'+id);const c=data.character;content.innerHTML='<div class="detail"><div class="panel"><h2>'+escape(c.name)+'</h2><p class="muted">'+escape(c.qqUserId)+' · '+escape(translated('playerStatus',c.status,'未知状态'))+'</p><p>等级 '+c.level+'｜境界 '+c.realmStage+'｜'+escape(translated('activity',c.activity,'未知状态'))+'</p><p>'+escape(c.region.name)+'（'+c.position.x+', '+c.position.y+', '+c.position.z+'）</p><div class="actions">'+(role==='viewer'?'':'<button data-audit="角色" data-id="'+c.id+'">核查角色</button><button data-audit="背包" data-id="'+c.id+'">核查背包</button><button data-audit="技能" data-id="'+c.id+'">核查技能</button><button data-audit="状态" data-id="'+c.id+'">核查状态</button>')+'</div></div><div class="panel"><h3>战斗面板</h3><p>生命 '+c.hp.current+'/'+c.hp.max+'</p><p>魔力 '+c.mp.current+'/'+c.mp.max+'</p><p>铜币 '+c.copper+'｜技能点 '+c.skillPoints+'</p><p class="muted">职业 '+escape(c.profession?translated('profession',c.profession,'主职业'):'未选择')+' / '+escape(c.secondaryProfession?translated('secondaryProfession',c.secondaryProfession,'副职业'):'无')+'</p></div></div><div class="two"><div class="panel"><h3>背包（前 120 条）</h3><ul class="list">'+(data.inventory.map(item=>'<li>'+escape(item.name)+' × '+item.quantity+'</li>').join('')||'<li>空</li>')+'</ul></div><div class="panel"><h3>装备与技能</h3><ul class="list">'+(data.equipment.map(item=>'<li>'+escape(translated('equipment',item.slot,'装备栏位'))+'：'+escape(item.name)+'</li>').join('')||'<li>未装备</li>')+data.skills.map(skill=>'<li>'+escape(skill.name)+' 等级 '+skill.level+'</li>').join('')+'</ul></div></div><div class="panel"><h3>最近玩家事件</h3><ul class="list">'+(data.events.map(event=>'<li>'+date(event.createdAt)+' · '+escape(translated('playerEvent',event.type,'玩家事件'))+'</li>').join('')||'<li>暂无记录</li>')+'</ul></div>'}

async function world(){title.textContent='世界';const data=await request('/api/admin/world');const writable=role!=='viewer';content.innerHTML='<div class="grid">'+metric('活跃战斗',data.active.battles)+metric('旅行',data.active.travels)+metric('公共场景',data.active.scenes)+'</div><div class="panel"><h2>全局倍率</h2><div class="actions">'+data.settings.map(item=>'<span class="tag">'+escape(translated('setting',item.key,'世界参数'))+'：'+item.value+'</span>'+(writable&&['experience_multiplier','drop_multiplier','copper_multiplier'].includes(item.key)?'<button class="secondary" data-multiplier="'+escape(item.key)+'" data-current="'+item.value+'">修改</button>':'')).join('')+'</div></div><div class="panel"><h2>区域生态</h2><table><thead><tr><th>区域</th><th>危险等级</th><th>开放</th><th>怪物</th><th>资源</th></tr></thead><tbody>'+data.regions.map(row=>'<tr><td>'+escape(row.name)+'</td><td>'+row.dangerLevel+'</td><td>'+(row.enabled?'是':'否')+'</td><td>'+row.monsters+'</td><td>'+row.resources+'</td></tr>').join('')+'</tbody></table></div>'}

async function gameOperations(){title.textContent='游戏内管理记录';content.innerHTML='<div class="panel"><div class="toolbar"><input id="game-operation-keyword" placeholder="管理者、玩家或操作内容"><button id="search-game-operations">搜索</button></div><div id="game-operation-table" class="empty">正在查询…</div></div>';const load=async()=>{const keyword=document.getElementById('game-operation-keyword').value.trim();const data=await request('/api/admin/game-operations?keyword='+encodeURIComponent(keyword));document.getElementById('game-operation-table').innerHTML=data.entries.length?'<table><thead><tr><th>时间</th><th>管理者</th><th>操作内容</th><th>目标玩家</th></tr></thead><tbody>'+data.entries.map(row=>'<tr><td>'+date(row.createdAt)+'</td><td>'+escape(row.operatorName)+'</td><td>'+escape(row.actionText)+'</td><td>'+escape(row.targetQqUserId||'—')+'</td></tr>').join('')+'</tbody></table>':'<div class="empty">暂无匹配的游戏内管理操作。</div>'};document.getElementById('search-game-operations').onclick=()=>load().catch(showError);await load()}

async function mails(){title.textContent='游戏邮件';const writable=role!=='viewer';const sender=writable?'<div class="panel"><h2>发放邮件</h2><p class="muted">角色编号留空即向全部玩家发放；每次发放都会写入审计总账。</p><div class="toolbar"><input id="mail-character" inputmode="numeric" placeholder="角色编号（留空为全服）"><input id="mail-title" maxlength="96" placeholder="邮件标题"><input id="mail-item" maxlength="64" placeholder="附件物品（可选：编号、代号、图鉴编号或名称）"><input id="mail-quantity" inputmode="numeric" value="1" placeholder="附件数量"></div><p><textarea id="mail-content" maxlength="4000" placeholder="邮件正文"></textarea></p><button data-send-mail="true">发放邮件</button></div>':'';content.innerHTML=sender+'<div class="panel"><div class="toolbar"><input id="mail-keyword" placeholder="角色、QQ 号、标题或正文"><button id="search-mails">搜索</button></div><div id="mail-table" class="empty">正在查询…</div></div>';const load=async()=>{const keyword=document.getElementById('mail-keyword').value.trim();const data=await request('/api/admin/mails?keyword='+encodeURIComponent(keyword));document.getElementById('mail-table').innerHTML=data.length?'<table><thead><tr><th>接收者</th><th>标题与附件</th><th>状态</th><th>接收时间</th></tr></thead><tbody>'+data.map(row=>'<tr><td>'+escape(row.characterName)+'<br><span class="muted">'+escape(row.qqUserId)+'</span></td><td>'+escape(row.title)+(row.attachments?'<br><span class="muted">附件：'+escape(row.attachments)+'</span>':'')+'</td><td>'+(row.deletedAt?'已删除':row.claimedAt?'已领取':'未领取')+'</td><td>'+date(row.receivedAt)+'</td></tr>').join('')+'</tbody></table>':'<div class="empty">暂无匹配的游戏邮件。</div>'};document.getElementById('search-mails').onclick=()=>load().catch(showError);await load()}

async function worldEvents(){title.textContent='世界奇遇';content.innerHTML='<div class="panel"><div class="toolbar"><input id="world-event-keyword" placeholder="奇遇名称、区域或旅人"><button id="search-world-events">搜索</button></div><div id="world-event-content" class="empty">正在查询…</div></div>';const load=async()=>{const keyword=document.getElementById('world-event-keyword').value.trim();const data=await request('/api/admin/world-events?keyword='+encodeURIComponent(keyword));const scenes=data.scenes.length?'<table><thead><tr><th>公共奇遇</th><th>位置与参与者</th><th>状态</th><th>时间</th></tr></thead><tbody>'+data.scenes.map(row=>'<tr><td>'+escape(row.title)+(row.weather?'<br><span class="muted">天气：'+escape(row.weather)+'</span>':'')+'</td><td>'+escape(row.regionName)+'（'+position(row.position)+'）<br><span class="muted">发现者：'+escape(row.discoverer)+'；参与者：'+row.participants+'</span></td><td>'+escape(translated('encounterStatus',row.status,'状态已记录'))+'</td><td>开启 '+date(row.openedAt)+'<br><span class="muted">截止 '+date(row.expiresAt)+'</span></td></tr>').join('')+'</tbody></table>':'<p class="empty">暂无公共奇遇场景。</p>';const encounters=data.encounters.length?'<table><thead><tr><th>旅人奇遇</th><th>旅人</th><th>区域</th><th>状态</th><th>时间</th></tr></thead><tbody>'+data.encounters.map(row=>'<tr><td>'+escape(row.title)+(row.weather?'<br><span class="muted">天气：'+escape(row.weather)+'</span>':'')+'</td><td>'+escape(row.characterName)+'</td><td>'+escape(row.regionName)+'</td><td>'+escape(translated('encounterStatus',row.status,'状态已记录'))+'</td><td>'+date(row.openedAt)+'</td></tr>').join('')+'</tbody></table>':'<p class="empty">暂无旅人奇遇进程。</p>';const ledger=data.ledger.length?'<table><thead><tr><th>事件</th><th>关联对象</th><th>区域与位置</th><th>结果</th><th>时间</th></tr></thead><tbody>'+data.ledger.map(row=>'<tr><td>'+escape(translated('worldEvent',row.type,'世界状态记录'))+'</td><td>'+escape(row.subject||row.actorName||'世界系统')+'</td><td>'+escape(row.regionName||'—')+(row.position?'<br><span class="muted">'+position(row.position)+'</span>':'')+'</td><td>'+escape(translated('worldOutcome',row.outcome,'已记录'))+'</td><td>'+date(row.createdAt)+'</td></tr>').join('')+'</tbody></table>':'<p class="empty">暂无世界事件记录。</p>';document.getElementById('world-event-content').innerHTML='<h2>公共奇遇场景</h2>'+scenes+'<h2>旅人奇遇进程</h2>'+encounters+'<h2>世界事件记录</h2>'+ledger};document.getElementById('search-world-events').onclick=()=>load().catch(showError);await load()}

async function patrolEntities(){title.textContent='巡游实体';content.innerHTML='<div class="panel"><div class="toolbar"><input id="patrol-keyword" placeholder="名称、区域或状态"><button id="search-patrols">搜索</button></div><div id="patrol-table" class="empty">正在查询…</div></div>';const load=async()=>{const keyword=document.getElementById('patrol-keyword').value.trim();const data=await request('/api/admin/patrol-entities?keyword='+encodeURIComponent(keyword));document.getElementById('patrol-table').innerHTML=data.length?'<table><thead><tr><th>实体</th><th>状态</th><th>当前位置</th><th>当前任务</th><th>调度时间</th></tr></thead><tbody>'+data.map(row=>'<tr><td>'+escape(row.name)+'</td><td>'+escape(translated('patrolStatus',row.status,'状态已记录'))+'</td><td>'+escape(row.regionName)+'<br><span class="muted">'+position(row.position)+'</span></td><td>'+escape(row.currentPoint)+(row.sceneId?'<br><span class="tag">关联公共奇遇</span>':'')+'</td><td>上次 '+date(row.lastActionAt)+'<br><span class="muted">下次 '+date(row.nextActionAt)+'</span></td></tr>').join('')+'</tbody></table>':'<div class="empty">没有匹配的巡游实体。</div>'};document.getElementById('search-patrols').onclick=()=>load().catch(showError);await load()}

async function journals(){title.textContent='审计总账';content.innerHTML='<div class="panel"><div class="toolbar"><input id="journal-keyword" placeholder="操作者、动作、原因或目标编号"><button id="search-journals">搜索</button></div><div id="journal-table" class="empty">正在查询…</div></div>';const load=async()=>{const keyword=document.getElementById('journal-keyword').value.trim();const data=await request('/api/admin/journals?keyword='+encodeURIComponent(keyword));document.getElementById('journal-table').innerHTML=data.entries.length?'<table><thead><tr><th>时间</th><th>操作者</th><th>动作</th><th>目标</th><th>风险</th><th>状态</th></tr></thead><tbody>'+data.entries.map(row=>'<tr><td>'+date(row.createdAt)+'</td><td>'+escape(row.actor)+'</td><td>'+escape(translated('action',row.action,'后台操作'))+'</td><td>'+escape(targetText(row.target))+'</td><td><span class="tag">'+escape(translated('risk',row.risk,'未知风险'))+'</span></td><td>'+escape(translated('operationStatus',row.status,'未知状态'))+'</td></tr>').join('')+'</tbody></table>':'<div class="empty">暂无匹配操作。</div>'};document.getElementById('search-journals').onclick=()=>load().catch(showError);await load()}

async function monitor(){title.textContent='监控告警';const data=await request('/api/admin/monitor');content.innerHTML='<div class="panel"><h2>未恢复告警</h2>'+(data.alerts.length?'<table><thead><tr><th>规则</th><th>等级</th><th>次数</th><th>最近发生</th></tr></thead><tbody>'+data.alerts.map(row=>'<tr><td>'+escape(translated('rule',row.rule,'系统告警'))+'</td><td><span class="tag '+escape(row.severity)+'">'+escape(translated('severity',row.severity,'未知等级'))+'</span></td><td>'+row.occurrences+'</td><td>'+date(row.lastSeenAt)+'</td></tr>').join('')+'</tbody></table>':'<p class="empty">当前没有未恢复告警。</p>')+'</div><div class="panel"><h2>任务心跳</h2><table><thead><tr><th>任务</th><th>状态</th><th>完成时间</th><th>耗时</th></tr></thead><tbody>'+data.jobs.map(row=>'<tr><td>'+escape(translated('job',row.code,'系统任务'))+'</td><td>'+escape(translated('jobStatus',row.status,'未知状态'))+'</td><td>'+date(row.finishedAt)+'</td><td>'+escape(row.durationMs??'—')+' 毫秒</td></tr>').join('')+'</tbody></table></div>'}

async function accounts(){title.textContent='管理员账号';const data=await request('/api/admin/accounts');content.innerHTML='<div class="panel"><h2>创建管理员</h2><div class="toolbar"><input id="new-user" placeholder="小写账号"><input id="new-pass" type="password" placeholder="初始密码（至少 10 位）"><select id="new-role"><option value="admin">管理员</option><option value="viewer">只读观察员</option></select><button id="create-account">创建</button></div></div><div class="panel"><table><thead><tr><th>账号</th><th>角色</th><th>状态</th><th>最后登录</th><th>操作</th></tr></thead><tbody>'+data.map(row=>'<tr><td>'+escape(row.username)+'</td><td>'+escape(translated('role',row.role,'未知角色'))+'</td><td>'+(row.enabled?'启用':'禁用')+'</td><td>'+date(row.lastLoginAt)+'</td><td>'+(row.username==='admin'?'—':'<button class="secondary" data-account="'+escape(row.username)+'" data-enabled="'+(!row.enabled)+'">'+(row.enabled?'禁用':'启用')+'</button>')+'</td></tr>').join('')+'</tbody></table></div>';document.getElementById('create-account').onclick=async()=>{try{await request('/api/admin/accounts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('new-user').value,password:document.getElementById('new-pass').value,role:document.getElementById('new-role').value})});await accounts()}catch(error){showError(error)}}}

async function open(page){try{if(page==='dashboard')await dashboard();else if(page==='players')await players();else if(page==='world')await world();else if(page==='world-events')await worldEvents();else if(page==='patrol-entities')await patrolEntities();else if(page==='game-operations')await gameOperations();else if(page==='mails')await mails();else if(page==='journals')await journals();else if(page==='monitor')await monitor();else if(page==='accounts')await accounts();else if(page==='logout'){await request('/api/admin/session',{method:'DELETE'});location.reload();return}document.querySelectorAll('.nav').forEach(button=>button.classList.toggle('active',button.dataset.page===page))}catch(error){showError(error)}}

document.addEventListener('click',async event=>{const target=event.target.closest('button');if(!target)return;if(target.dataset.page)return open(target.dataset.page);if(target.dataset.player)return player(target.dataset.player).catch(showError);if(target.dataset.audit){try{const reason=prompt('请填写核查原因（将写入审计总账）：')||'';const result=await request('/api/admin/players/'+target.dataset.id+'/audit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:target.dataset.audit,reason})});alert([result.fixed,'操作编号：'+result.operationId].join(String.fromCharCode(10)))}catch(error){showError(error)}}if(target.dataset.multiplier){try{const value=prompt('请输入 '+translated('setting',target.dataset.multiplier,'世界参数')+'的新倍率（0～20）：',target.dataset.current);if(value===null)return;const reason=prompt('请填写修改原因（将写入审计总账）：')||'';await request('/api/admin/world/multiplier',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({key:target.dataset.multiplier,value,reason})});await world()}catch(error){showError(error)}}if(target.dataset.sendMail){try{const characterId=document.getElementById('mail-character').value.trim();const title=document.getElementById('mail-title').value.trim();const contentText=document.getElementById('mail-content').value.trim();const item=document.getElementById('mail-item').value.trim();const quantity=document.getElementById('mail-quantity').value.trim();const reason=prompt('请填写发放原因（将写入审计总账）：')||'';if(!reason)return;if(!confirm(characterId?'确认向该角色发放邮件？':'确认向全部玩家发放邮件？'))return;const result=await request('/api/admin/mails',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({characterId,title,content:contentText,item,quantity,reason})});alert('已向 '+result.recipients+' 名玩家发放邮件。'+String.fromCharCode(10)+'操作编号：'+result.operationId);await mails()}catch(error){showError(error)}}if(target.dataset.account){try{await request('/api/admin/accounts/'+encodeURIComponent(target.dataset.account),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({enabled:target.dataset.enabled==='true'})});await accounts()}catch(error){showError(error)}}});
open('dashboard');`;

export const loginPage = (nonce: string) => pageShell('梨子喵的世界观测台', `<main class="login"><section class="intro"><div class="eyebrow">安全管理后台</div><h1>梨子喵<br>の<br>世界观测台</h1><p>从这里查看旅人的轨迹、世界的脉动与每一次受控操作。<br>所有管理行为都会留下可追溯的记录。</p></section><section class="login-panel"><form class="card" id="login-form"><h2>登录</h2><label class="field"><span>账号</span><input name="username" autocomplete="username" value="admin" required></label><label class="field"><span>密码</span><input name="password" type="password" autocomplete="current-password" required></label><button style="width:100%;margin-top:22px" type="submit">安全登录</button><div class="error" id="error" aria-live="polite"></div></form></section></main>`, `const form=document.getElementById('login-form');form.addEventListener('submit',async event=>{event.preventDefault();const error=document.getElementById('error');error.textContent='';const data=Object.fromEntries(new FormData(form));try{const response=await fetch('/api/admin/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.message||'登录失败。');location.reload()}catch(err){error.textContent=err.message||'登录失败。'}});`, nonce);

export const adminPage = (session: WebSession, nonce: string) => pageShell('梨子喵的世界观测台', `<main class="app"><aside class="side"><div class="brand">梨子喵的世界观测台<small>管理后台</small></div><button class="nav active" data-page="dashboard">总览</button><button class="nav" data-page="players">玩家</button><button class="nav" data-page="world">世界</button><button class="nav" data-page="world-events">世界奇遇</button><button class="nav" data-page="patrol-entities">巡游实体</button><button class="nav" data-page="game-operations">游戏内管理记录</button><button class="nav" data-page="mails">游戏邮件</button><button class="nav" data-page="journals">审计总账</button><button class="nav" data-page="monitor">监控告警</button>${session.role === 'owner' ? '<button class="nav" data-page="accounts">管理员账号</button>' : ''}<button class="nav" data-page="logout">退出登录</button></aside><section class="main"><div class="top"><div><h1 id="title">总览</h1><div class="who">${session.username} · ${adminRoleLabel(session.role)}</div></div><span class="tag">已启用审计</span></div><div id="content"><div class="empty">正在读取数据…</div></div></section></main>`, clientScript(session.role), nonce);
