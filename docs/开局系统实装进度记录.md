# 开局系统实装进度记录

更新：2026-09-08。状态：开发中，尚未达到完整发布或验收条件。

## 2026-09-08：设计差异与新手安全指引复核

最新结论见[设计差异与新手安全指引复核](./开局系统设计差异与新手安全指引复核.md)。以下较早阶段的验证记录保留为历史，不代表最新覆盖范围。

- 修复未触发剧情页面的无效继续按钮，并兼容旧翻页按钮启动；抵达后先引导入会，再办理凭物与选职。
- 出生与护送结算共用安全公会校验，排除关闭、私人、会刷怪、缺公会、缺地图物品及坐标不属于地图的终点。全部不可用时回滚并保留状态。
- 修复猩红王冠混穿时被另一套史诗计数覆盖，单件／两件护持与摘要一致；G08 交涉名称统一为万语聆听。
- 当前配置 10 图开放给普通玩家、4 图未开放。隔离库完成 14 图 42 路线 113 分支的注册、护送、入会与凭物交接，到 Lv.3 后指向职业或女神事项。
- 类型检查通过；相关回归 51 项通过，三份隔离 MySQL 测试 15 项通过，无跳过。属性重算和开化档案使用替身，未做真实 QQ 客户端或完整多人并发验收；未修改正式地图开关或重启服务。
- 六项神技、随从生产专长、Lv.5 后外地主线等仍未完成；按用户要求暂缓的额外大厅服务不算作遗漏。

## 2026-09-08 后续补全与现场报错修复

- V3 的 54 个基础分支已配置独立的经办 NPC、窗口、对白与档案结果。新增凭物核验、教学记录、归档／恢复显示件、原件交存、事故函转回执，以及限次维修、餐食、住宿、额外交通服务。魔界邀请函另有核验入口。对应服务由安全公会转接，后期地图仍按设计保留为伏笔。
- 抵达／登记／教学经验按 `E(1)+E(2)` 的 40%／20%／40% 写入一次性记录；固定经验不乘全局倍率。升等后补足生命和魔力。主线继续检查凭物交接、职业与安全区 Lv.5 之前的两步委托。
- 阿库娅的三章落地登记已接入 `/女神`：净水协作身份、三选一安顿、核对范围并完成三桶净水。结算一次 40 铜币，同行与暂驻有世界记录，同行占用普通随从的出行位。低频战斗支援与许可成长在下方本次补全记录中说明。
- 初始目的地停航时会查找开放的安全公会改道；替换到达对白。厄里斯版 A01 保留事故经过，只替换神界接引。
- 注册重复按键恢复当前页面；新“继续”按钮携带原页面标记，避免连点跳页。注册正文保存到 `registration_scene_records`，天堂页也保存状态；恩赐选择已提交后重试，恢复领取回执或当前剧情，不重新发奖。公会委托和女神动作另存事务内重放记录。
- 按玩家最后要求，神技目录保留原有正文蓝色链接与三行翻页／分类按钮。蓝色链接 `autoEnter:false`，只填入指令，不直接发送；不会再生成被 QQ 拒绝的 `qqbot-cmd-enter ... show=...`。
- 修复恩赐选择时的 MySQL 1442：原穿戴 `INSERT ... SELECT ... FROM player_item_instances` 与装备绑定触发器冲突。改为读取物品定义后，以 `VALUES` 创建实例、穿戴，触发器继续执行个人绑定；缺少初始装备会回滚事务。

本节与下方补全记录说明源码现状，不能代替整套验收。54 项服务、女神三章和安全改道尚需完整数据库路径验收。

### 前一阶段已执行验证

- `npx tsc --noEmit` 通过；注册、开局内容、QQ 消息转换与鉴识升级四份测试合计 20 项通过。
- `test/opening-message.test.ts` 使用当前安装的 QQ 适配器转换函数核验目录、分类、搜索和详情，检查蓝色链接使用 `cmd-input`、18 个神技入口完整、键盘不被截断。
- 启用 `FF_OPENING_DB_TEST=1` 执行 `test/opening-registration.integration.test.ts`：1 个父测试和 3 个子测试通过，无跳过。在新建的隔离 MySQL 库安装源码中的实际绑定触发器，复现旧 SQL 的 1442，验证新 SQL 穿戴成功及缺件整批回滚。临时库已清理。
- 该集成测试只验证初始装备发放片段及触发器，并非完整注册或 113 分支端到端验收。未对生产库执行初始化／迁移，未重启服务或发送真实 QQ 测试消息。

## 2026-09-08：继续补全及世界树前台调整

### 公会与路线奖励

- 人物交互按指定样式统一：标题「冒险者公会·前台／商店／餐厅」、姓名与同行蓝色「详情」、场景和对白引用块。世界树使用维萝、斐珞、朵菈和其余本地人物；闲聊只发送人物消息，不再追加大厅。「详情」沿用 `/域民详情`，指向对应公会人物，仍须识珠 Lv.3 且在当地公会；蓝色链接 `autoEnter:false`。

- 按玩家最后要求，世界树大厅严格参考百纳镇，保留四行七键：前台／集结区、悬赏板／委托板、餐厅／工会商店、离开公会。撤下大厅的礼包、教学、热食、恢复、维修、人物、接驳及翻页入口；额外服务后续确有需要时再安排。旧「次页」指令返回同一简洁大厅。

- 世界树前台按百纳镇的业务顺序排列：第一行「冒险者 注册／晋升、职业选择」，第二行「闲聊 维萝、返回公会大厅」。大厅增加前台入口；登记、选职后的返回落在当地前台。世界树闲聊使用维萝的已有首见／再见对白，注册卡片场景也改为维萝。没有为尚未具备切磋入口的角色生成无效切磋按钮。
- 世界树人物页改为每行两人；满员随从名册也压在 QQ 五行限制内。人物、邀请、返回入口均经过实际安装版本的 QQ 转换函数检查。
- 本地商店使用世界树斐珞等对应接待文案；悬赏接取／领取和职业指引接入当前安全公会。服务层校验当前位置、进入状态及战斗、交涉、移动状态。
- R医补回魔药 2 份；R食额外热食 3 份；R匠木／石／金属各 3 份及维修 1 次；R商普通补给抵扣 150 铜币；R探勘路笔记与普通低危地图兑换 1 次；R契基础疗养 1 次；R艺工艺凭单及安全练习 1 次。每角色按记录补一次，公会可核对与补领。**只适用于仍保存 `choice.pack` 的 F03 和 V2 七图；V3 六图继续使用专属凭物，不混入旧礼包。**
- R守护符和一次救援额度已登记，但当前普通 PVE 没有收费救援环节，额度尚无实际核销入口；不能把这项写成完整兑现。
- 新开局角色选职后领取适配职业的普通 Lv.1 个人绑定武器；已经选职的新开局角色可在公会补领，重复操作不会多发。原先没有新开局记录的旧角色不会因此收到奖励。
- 已写入「礼包与凭单」服务代码；按最后要求不放入世界树大厅入口。低危地图兑换检查实际背包及仓储持有情况。凭单练习给出实际成品并登记教学，基础维修限 Lv.5 及以下普通装备；灵契饲料可持续在公会商店购买。补给额度购买所得设为个人绑定，防止免费额度转售。
- 黄金宝箱奖励表升级为 V2：医疗档补回魔药 2 份，商业档补给券改为 1 张。已经生成的 V1 确认凭据仍按 V1 开奖，不改概率、不重新抽取。凭物显示件恢复同时检查背包和家园仓储。

### 神技、装备、随从与女神

- G01 接入休息恢复和每日一次败战后恢复 50% 生命；G02 接入坐标步行计时；G06 补上背包食物持续时间；G08 接入初次交涉偏好提示和喜欢物品的增幅；G09 补上冒险亲密；G10 目录报价与扣款共用额度；G12 接入非固定角色经验。G08 的增幅仍在现有单次／总心情上限前计算，Boss 心情展示继续以掉落总价值为基准。
- 共用战斗规则接入 G15 普通敌对 PVE 直击增伤、G16 技能 MP 折减、G17 对应直击减伤；G14 接入原生法术治疗计算，固定百分比回血不额外乘算。G13 接入普通组队 PVE 的随机掉落，固定掉落和剧情奖励不加成。下表保留尚未覆盖的来源与无现有消耗入口的设计。
- 初晓长剑首个有效普攻护盾、短刃按实际损血追加伤害、轻跃拳套首次受击后蓄势、晨枝法杖首次实付 MP 返还、掌心微日首次有效治疗追加恢复均已接入，次数随战斗状态保存。猩红王冠按防具槽计数，单件／两件低血护盾及五件休息恢复接入；切磋与测试场不触发这些开局装备的 PVE 特效。初晓书的原定鉴识免耗没有独立增益，见下表。
- 普通 PVE 中随从每三次有效主人行动检查支援，手动与自动共用；行动重试不重复计数。黄金兔低血护持、普通随从攻击、范围受伤后的独立退场、败战受伤、胜利经验与冒险亲密已接入。随从不继承主人的神技／装备特效，不增加独立掉落份额。
- 寻物专长接入实际移动后的普通空地事件，每日最多三次寻药；守望专长提高普通休息恢复。黄金兔达到亲密要求后，可在当地公会每日享用一次恢复体力的面包。采集／工坊专长的生产效果尚未接入，不能视为全部专长完成。
- 阿库娅同行时每三次有效主人行动检查支援，每场最多一次低血治疗和一次普通负面状态净化；净化不清除机制状态。许可从 Lv.1 开始，10／20 场有效普通胜利成长至 Lv.2／3，治疗为最大生命的 5%／6%／7%，上限明确。同行仍受世界唯一归属与随从出行位互斥约束。
- 剧情中间件只对白名单中的完整命令或参数边界放行，避免相似前缀绕过；未触发开局时，仅首次移动／寻怪启动事件。全服务入口审查仍未结束。

### 本轮验证与边界

- `npx tsc --noEmit` 通过；十份相关回归测试共 82 项通过（大厅精简前），无失败或跳过。覆盖开局内容、神技／装备实际规则、QQ 转换、注册重放、职业指引、鉴识费用、交涉规则／消息、Boss 部位与甲类效果。
- `test/opening-effects.test.ts` 的 12 项检查覆盖伤害来源隔离、减伤上限、实付 MP、专属装备次数、实际损血、有效治疗、套装识别、随从行动去重与独立受伤、女神支援成长、宝箱旧版本和商店额度。
- `test/opening-message.test.ts` 的前 4 项检查使用本机 QQ 适配器，包含世界树前台未注册／已注册的按钮、维萝闲聊路由、注册回执返回，以及与百纳镇首行标签、操作、样式的一致性。
- 大厅精简后，重新执行 `npx tsc --noEmit` 通过；消息测试先有 5 项全部通过。新增检查将世界树大厅、旧次页、集结区与委托板经 QQ 转换后的按钮分行、标签及样式与实际百纳镇大厅比较，确认均为四行七键，已有补给额度也不会插入额外服务按钮。
- 人物排版补全后，消息测试 6 项通过，新增验证覆盖八名公会人物的引用对白、详情链接、当地及鉴识限制，并检查世界树商店、餐厅实际入口的接待人。
- 隔离 MySQL 路线奖励测试实际执行，1 个父测试和 4 个子测试通过，无跳过：八类额外礼包、个人绑定、缺项整批回滚、并发补领不重复、选职武器实例与一次性领取。临时库已清理。
- 隔离 MySQL 交涉测试实际执行，1 个父测试和 10 个子测试通过，无跳过：含本次 G08 与交涉专长首次赠礼叠加、后续赠礼、上限与重放不扣第二份物品。临时库已清理。
- `git diff --check` 未报空白错误。检查使用了隔离替身或临时数据库，**未做完整战斗／113 分支端到端验证，未执行生产初始化或迁移，未重启服务，未发送真实 QQ 消息。**

## 设计覆盖

- V1：系统规则和幽暗密林 F01～F03。
- V2：S、D、H、I、W、B、Y 七组，共 21 条路线。
- V3：M、R、A、C、T、E 六组，共 18 条路线，替代对应 V2 故事。
- 合计 14 张初始地图、42 条路线、113 个互斥分支。
- 浮叶镇、雪灯坳、霜龙客舍、眠鲸旅市为剧情安全终点，不自动进入出生池。地下迷宫、测试与私人地图不属于本轮出生路线范围。

## 已写入的代码

1. MD 内容编译器、42 条运行时路线、移动／寻怪衔接和分页文案；补充森林和部分 V3 结束台词。
2. 注册的 18 项神技目录、技能限定、已开放地图分档抽取、角色级路线存档、首次行动展示与进度版本校验。
3. 分支领奖、黄金兔、黄金宝箱、猩红王冠随机部位、魔族少女救援草药确认与入镇替代场景。
4. 随从名册、接纳／婉拒、照料、改名、放归确认及交涉成功邀请接口。邀请按心情占上限的比例判定，Boss 不应用 G09 邀请倍率。
5. 黄金宝箱固定及权重奖励表、批量 1～100、确认页面、存储开奖、承载检查、个人绑定、结果分页和重放。
6. 公会上下文、世界树独立人物、基础接待／热食／修理／教学／见闻及安全接驳入口。商店与餐厅的服务层增加实际地点检查。
7. 全局女神和浮叶航路的持久记录；航路在落脚交接时登记。G06 餐厅持续时间、G10 购买扣款接口已写入。

这里的“已写入”不等于数据库与玩家客户端验证完成。

### 2026-09-08：鉴识随降临绑定

- 阿库娅、厄里斯均在临行剧情说明异世界的危险，授予通用鉴识；玩家仍另外选择一项神技。
- 新角色直接获得鉴识 Lv.1、慧眼 Lv.1、识珠 Lv.1，初始 1 SP 完整保留；去掉主线“学习鉴识”门槛和旧学习提示。
- 后续慧眼与识珠继续由玩家自行消耗技能点升级，费用和上限沿用原规则；绑定基础技能不会因技能点重置被遗忘。
- 初始化逻辑只为缺少鉴识的旧角色补齐基础技能与缺失的专精记录，保留已有升级；尚未在真实数据库执行。
- 18:57 验证：`npx tsc --noEmit` 通过；开局、职业指引、技能升级费用三份测试共 23 项通过。覆盖降临免费授予、旧学习入口不扣费、鉴识后续升级扣费、SP 不足与专精上限；这些检查使用隔离替身，不代表真实数据库或客户端验收。

## 仍须完成，不能按全部实装交付

以下按 2026-09-08 本次源码复核更新，已移除过时的“尚未接入”表述。

| 模块 | 明确未完成的内容 |
| --- | --- |
| 神技效果 | G03、G04、G05、G07、G11、G18 尚缺实际效果；G12 的职业熟练度未接入；G14 其他治疗提供方及 G16 绕过共用规则的特殊资源消耗仍需逐项审查，不能称全部技能覆盖。 |
| 现有机制与设计冲突 | 坐标步行当前不扣体力、鉴识不耗 MP、普通 PVE 败战没有救援费，因此 G02 体力折扣、G01 救援折扣、初晓书鉴识免耗、R守救援额度没有新的实际收益。未为了让减免生效而新增收费；需另行确定对应替代收益。 |
| 随从玩法 | 采集专长缩时、工坊专长每日材料节省尚未接入；更多物种的不同攻击／治疗方式、所有范围技能的受伤处理需补全。手动与自动完整战斗结算尚未完成数据库端到端验收。 |
| 独立主线 | 路线各自的鉴识、治疗、工艺、勘路教学尚未全部形成独立任务步骤；Lv.5 后外地出身与后续主线的衔接仍需补全。 |
| 分会与城镇 | 世界树前台、人物页、职业指引和悬赏／商店基本入口已适配；新城镇仍缺完整独立日常活动。 |
| 女神后续 | 三章、支援与许可成长已有代码；事故函已转回执，但本人阅读及答复状态没有完整推进流程。 |
| 凭物细节 | 54 个分支已有服务配置及核验／归档入口；部分地图服务仍统一列安全接引点，未细化为各分支调查线路。正常接驳目前免费无限次，额外交通凭物收益未与基础返程规则区分。 |
| 保护与兼容 | 出生点还缺水域／Boss／建筑占位筛查；服务层全部动作入口的保护审查未结束；宝箱 V1／V2 已兼容，但运行中旧故事版本保留策略仍需补全。 |

### 已接入，但尚未完成整体验证

- 54 项凭物服务、女神三章、固定教学经验、安全委托、目的地改道与本地到达对白：源码已有，不再计为“完全未实装”，但未逐条完成数据库与客户端验收。
- 113 个互斥分支从注册到下一条可执行主线的全流程；多人争取女神、首次公开浮叶航路、重复初始化、失败重试和旧角色兼容。
- 剧情全部动作入口的保护、负重与奖励预算、凭物兑换并发、交通停航、跨地图高度、QQ 页长及按钮完整性。
- 已执行的隔离 MySQL 验证包含初始装备绑定触发器、额外礼包与职业武器、交涉与神技赠礼加成；不能据此宣称整套注册、全部凭物服务、完整战斗与全地图已经验证通过。

王都、魔界、精灵王庭、天穹等设计明确标为“后续伏笔”的正式章节暂未开放；这部分与本期承诺尚未兑现的缺口分开计算。

## 早期开发验证记录（历史）

- 修改前 `npx tsc --noEmit` 通过。
- 本次开发中已修复新增代码编译错误，多次 `npx tsc --noEmit` 通过；继续修改后仍需最终重跑。
- `test/opening.test.ts` 的 5 项检查通过：14 图 42 路线 113 分支完整字段、指定新版故事、armed 页读取、随从心情比例边界、宝箱批量概率与数量。
- 原有职业测试的隔离替身已补充无新开局时的依赖，原有 9 项职业检查通过。其他选定的交涉、消息、甲类和人偶检查在最近组合运行中通过；组合运行当时发现缺少个别结束台词，随后修复并单独复跑开局检查通过。
- 18:20 最后复跑：`npx tsc --noEmit` 通过；上述开局、职业、交涉规则／消息、甲类和人偶消息六份测试合计 40 项通过，无失败或跳过。
- AlemonJS 在测试进程内保留后台句柄，普通测试命令曾无法退出并被中断。验证改用 Node 的 `--test-force-exit`；没有将被中断的运行记录为通过。
- 未执行真实数据库初始化或迁移，未做完整临时 MySQL 库集成与并发验证，未发送真实客户端消息，未部署或重启生产服务。

## 后续接入位置

- 内容：`scripts/generate-opening-content.ts`、`src/game/opening-content.generated.ts`、`src/game/opening-content.ts`。
- 状态：`src/game/opening.service.ts`、`src/game/opening-state.ts`、`src/middleware/opening.ts`。
- 公会：`src/game/guild-context.ts`、`src/game/opening-guild.service.ts`、`src/response/opening-guild.ts`。
- 随从：`src/game/companion.service.ts`；邀请、普通战斗支援、经验与受伤已有调用，仍需补生产专长和完整战斗验收。
- 神技与装备：`src/game/divine-effects.ts`、`src/game/opening-combat.ts`、`src/game/combat-rule-registry.ts`。
- 礼包：`src/game/opening-pack.service.ts`；女神支援：`src/game/aqua.service.ts`。
- 宝箱：`src/game/opening-chest.service.ts`、`src/game/opening-chest.config.ts`。
- 新增表由 `src/database/bootstrap.ts` 末尾的 opening、companions、opening-chests 初始化器管理。
- 数据库连接池 `getPool()` 会触发完整初始化；集成验证应复用现有临时数据库或事务注入办法，不直接连接生产池当作只读检查。
