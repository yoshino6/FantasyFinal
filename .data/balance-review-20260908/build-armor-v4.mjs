import {readFileSync,writeFileSync} from 'node:fs';
const d=JSON.parse(readFileSync('.data/balance-review-20260908/armor-v4.json','utf8'));
const pct=x=>(x*100).toFixed(2)+'%',n=x=>Number(x.toFixed(2)).toString(),sg=x=>(x>0?'+':'')+n(x)+'%',gain=x=>sg((x-1)*100);
const table=(h,rs)=>'| '+h.join(' | ')+' |\n| '+h.map(()=>'---').join(' | ')+' |\n'+rs.map(r=>'| '+r.join(' | ')+' |').join('\n');
const effect=s=>[['hit','命中修正'],['evade','闪避修正'],['hp','生命上限'],['mp','魔力上限'],['critAvoid','暴免修正'],['critReduction','暴抗修正'],['reduction','减伤']].filter(([k])=>s[k]).map(([k,v])=>v+'+'+n(s[k])+'%').join('；');
const rs=[];for(const [name,v] of Object.entries(d.profiles))for(const [slot,w] of [['上装／下装，每件',1.2],['头肩／腰部／脚部，每件',13/15]])rs.push([name,slot,...v.map(x=>sg(x*w))]);
const spread=Math.max(...d.full.map(x=>x.score))/Math.min(...d.full.map(x=>x.score))-1;
const worst=d.scenarios.toSorted((a,b)=>b.spread-a.spread)[0];
const text=`# 五类甲重量约束与单件套装数值 V4

> 历史建议稿：用户后续已提交新的单件表、套装表，并明确单件独立乘算。当前有效规则与核算见[用户甲类单件与套装乘算方案V7](用户甲类单件与套装乘算方案V7.md)，本稿的单件数值、加算方式与平衡结果不再作为当前方案。

日期：2026-09-08。状态：**数值设计稿，未修改游戏配置、源码或数据库。**本版取代V3甲类表，保留[原完整审计](玩家六维成长与装备甲类重平衡复核建议V1.md)中的成长1／2／3倍、六维转换不变与普通主词条2:1基准。

## 1. 先服从甲类定位，再校准数值

V3给轻／重／板甲正向命中与闪避，只是为了拉齐静态指标，违背了本项目的甲类定位，现撤销。

- **布甲：**柔软、活动空间大；保留机动优势，不凭空提供抗暴或抗冲击。
- **皮甲：**轻便且有一定护体能力；机动优势小于布甲，开始提供抗暴与韧性。
- **轻甲：**防护优于皮甲；不增加命中、闪避，速度有轻微代价。
- **重甲：**强化护体与稳定性；降低闪避与速度。
- **板甲：**最高护体与稳定性，同时承担最大的闪避、速度代价。

五类单件命中均为0%：重量不应让人瞄得更准，也没有必要把重量直接等同于攻击必然打不中。机动代价优先放在闪避、速度上，防护价值优先放在暴免、暴抗、韧性上。

这是符合项目设定的游戏抽象，不将这些数值宣称为现实护甲的物理定律。五类同双防、生命／魔力套装均是用户先前明确指定的游戏规则；本版保留。命中增强仅存在于用户指定的布／皮套装协同，不再来自每块护甲本身。

## 2. 最终单件表

以下为100%品质。每个部位提供表内数值，均作用于对应人物属性的**数值**，不是概率百分点。五件相加后生效，不能逐件连乘。

${table(['甲类','部位','命中','闪避','暴免','暴抗','韧性','速度'],rs)}

完整五件的单件附带属性合计：

${table(['甲类','命中','闪避','暴免','暴抗','韧性','速度'],Object.entries(d.profiles).map(([name,v])=>[name,...v.map(x=>sg(5*x))]))}

从布到板，闪避与速度单调下降，暴免／暴抗／韧性单调提高。板甲平均件暴免、暴抗各+16%的参考值仍保留。上装与下装各占24%预算，其他三个部位各占17.333…%；展示四舍五入，结算使用精确权重。

### 共同双防与品质

仍按五类同等级、同稀有度、同品质、同部位双防相同。普通100%的标准预算为：

~~~text
G(L)=min(9,L-1)+2min(10,max(0,L-10))+3min(10,max(0,L-20))，L=1—30
B(L)=8+4.3×(100+10G(L))/6
每件上装／下装：物防=魔防=0.12B(L)
每件头肩／腰部／脚部：物防=魔防=13B(L)/150
五件合计：物防=魔防=0.5B(L)
~~~

${table(['等级','上装／下装，每项防御各','小部位，每项防御各','五件每项防御合计'],[10,20,30].map(l=>{const g=Math.min(9,l-1)+2*Math.min(10,Math.max(0,l-10))+3*Math.min(10,Math.max(0,l-20)),b=8+4.3*(100+10*g)/6;return[l,n(.12*b),n(13*b/150),n(.5*b)];}))}

双防按既有稀有度倍率1／1.15／1.3／1.5／1.7／2与品质系数0.6+Q/250发挥。六项百分比继续采用固定比例上限、不随等级和稀有度重复放大的候选规则；品质决定发挥。负向代价与正向附带属性都乘品质系数。套装依旧只按件数触发，不乘品质或稀有度。

## 3. 最终3／5件套

定位遵循用户指定。5件覆盖3件，表内均为该档全部效果。

${table(['甲类','3件套','5件套'],Object.entries(d.sets).map(([name,s])=>[name,effect(s[3]),effect(s[5])]))}

**重甲与板甲这次按同一生存预算交换：**五件套暴免／暴抗修正均为16%。重甲25%生命；板甲20%生命＋4%减伤，在没有其他减伤时1.20/0.96=1.25，恰好等于重甲生命系数。三件分别1.125与1.10/0.98≈1.12245，也接近。

皮甲没有生命奖励，且命中对已高命中角色收益会递减，故将魔力定位提高到五件30%；轻甲为25%生命＋20%魔力。布甲只获得命中、闪避协同。没有再给轻／重／板套装添加命中或闪避。

### 概率修正保持当前计算方法

~~~text
最终命中=[p+(1-p)×命中修正]×(1-目标闪避修正)
最终暴击=原暴击概率×(1-目标暴免修正)
最终额外暴伤=原额外暴伤×(1-目标暴抗修正)
~~~

20%命中修正把50%变为60%，不是70%；暴抗修正只削减暴击额外部分。必中与强制暴击遵循当前优先级。板甲减伤进入当前减伤池，受总80%上限约束。

## 4. 拆件与全套复算

基线为同直接双防、关闭甲类附带属性与套装的配装；双方对抗属性相等。直击指标=相对期望直击输出×相对有效生命。它不是胜率，也未包含速度、控制、资源的价值。

${table(['甲类','一件小部位附带属性收益','一件衣裤附带属性收益','五件附带属性，关闭套装','3件套额外收益','5件套额外收益'],d.full.map(x=>[x.name,gain(x.smallSingle),gain(x.largeSingle),gain(x.noSet),gain(x.threeSetGain),gain(x.setGain)]))}

以上单件收益**未计共同双防**，不能读成“穿一件防具只变强1%”。例如敌人攻击等于躯体防御时，上装增加0.12倍躯体防御，单是双防带来的对应直击有效生命系数就为1.06；小部位为1.04333；五件合计为1.25。再乘甲类附带收益，衣裤单件的示例指标约+8%—9%。这些是特定攻防比例下的结果，不是固定减伤百分比。

${table(['甲类','命中率','敌方命中率','敌方暴击率','期望输出增益','有效生命增益','全套直击指标增益'],d.full.map(x=>[x.name,pct(x.hit),pct(x.incoming),pct(x.crit),gain(x.output),gain(x.ehp),gain(x.score)]))}

中性全套直击指标差距${pct(spread)}；皮、轻的一部分预算留给魔力。当前方案不会为让重甲命中超过50%，再次制造“越重越准”的属性。

### 功能属性独立看

同级、敌方破韧等于穿甲前韧性时，五件的控制成功率／DOT系数按现有公式为：

${table(['甲类','控制／DOT系数','速度变化'],d.full.map(x=>[x.name,pct(x.tenacity.controlChance),sg(x.total[5])]))}

韧性是当前游戏的综合抗性抽象，不能据此说板甲免疫毒素或精神控制。速度参与行动顺序及部分机制，不等于增加或减少固定比例的行动次数。

满魔、固定耗魔、无回魔，原可施放10次技能时：皮甲30%魔力可施放13次，轻甲20%可施放12次。用20次行动、技能期望伤害为普攻2倍的资源算例，皮甲额外总输出10%，轻甲6.67%；对应指标：

${table(['甲类','不计额外施法','资源算例'],d.mana.map(x=>[x.name,d.full.find(f=>f.name===x.name).score.toFixed(3),x.resourceAdjustedScore.toFixed(3)]))}

若战斗不缺蓝，这部分收益就是0；以上不是正式技能模拟，不能默认实战总能兑现。

## 5. 真实属性比例与极端场景

沿用2026-09-08 19:21:13北京时间的只读快照，没有重新连接数据库。42名人类玩家的新成长躯体投影，与同级无特性的普通／大型／精英怪形成5642组配对；不是5642场实战。按普通100%候选全套比较，排除其他装备、神器、技能、宠物、机巧与队伍效果。

${table(['等级','怪物','配对数','布','皮','轻','重','板','配对内差距中位数','P95'],d.actual.groups.map(g=>[g.label,{normal:'普通',large:'大型',elite:'精英'}[g.tier],g.pairs,...g.scores.map(s=>s.median.toFixed(3)),pct(g.medianSpread),pct(g.p95Spread)]))}

表中指标分别相对同一配对的关闭甲类效果基线，各列中位数独立统计；重复配对没有按线上遭遇频率加权。皮甲对高命中角色的短战收益偏低，要靠确实缺蓝时的资源优势补偿，不能宣称已完全等强。

独立变动攻守命中比例、暴击／暴伤比例与品质，共243种属性场景，加6种必中、强暴、无暴、高减伤场景，每种枚举3125套，共${d.checks.totalComparisons}次配装比较。当前直击模型内没有混搭超过同场景最佳全套；未证明实际技能或混合品质环境也成立。

极端属性场景最大差距${pct(worst.spread)}，最佳${worst.full[0].name}、最低${worst.full.at(-1).name}。参数：己方命中比${worst.s.outRatio}、敌方命中比${worst.s.inRatio}、暴击比${worst.s.critRatio}、暴伤比${worst.s.bonusRatio}、品质${worst.s.quality}。低暴击怪令重甲抗暴难以兑现；不能用违反甲类定位的正向命中闪避消灭这种差异。

已有70%减伤时，板甲额外4%使受伤从30%降到26%，边际有效生命提高15.38%，而不是4.17%；已有80%减伤时额外减伤不再生效。因此暂不提高板甲减伤或修改总上限。

## 6. 结论、验证与剩余工作

**建议用V4替换V3作为候选：先保证重量、机动、防护的方向一致，接受环境相关的强弱，不再为凑一个指标给重甲添加机动收益。**在同双防、固定套装定位的约束下，无法同时保证每个敌人、每个技能、每种缺件状态都完全等强。

普通装备体验应连同主词条双防、单件、3件与5件一起看。成长与怪物预算仍按原审计分别调整；不能让怪物也直接复制玩家1／2／3倍成长，抵消玩家提升。本轮未重新审计机巧或神器实战。

数学脚本断言验证了重量序列、非正命中／闪避约束、概率边界、部位权重及套装计件；没有改游戏源码。最终仍需验证技能命中保底、回魔、控制链、减伤叠加与有／无神器体验。生命／魔力套装增加上限，不应通过换装免费补满。

- [审计脚本](../.data/balance-review-20260908/armor-v4.ts)
- [计算结果](../.data/balance-review-20260908/armor-v4.json)
- [文档生成器](../.data/balance-review-20260908/build-armor-v4.mjs)
- 公式依据：[combat-math.ts](../src/game/combat-math.ts)、[combat-rule-registry.ts](../src/game/combat-rule-registry.ts)、[armor-set.ts](../src/game/armor-set.ts)。

复算：运行 npx tsx .data/balance-review-20260908/armor-v4.ts，再运行 node .data/balance-review-20260908/build-armor-v4.mjs。
`;
writeFileSync('docs/五类甲重量约束与单件套装数值V4.md',text);
console.log(JSON.stringify({document:'docs/五类甲重量约束与单件套装数值V4.md',chars:text.length,neutralSpread:spread}));
