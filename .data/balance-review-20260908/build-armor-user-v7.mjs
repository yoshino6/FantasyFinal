import {readFileSync,writeFileSync} from 'node:fs';
const d=JSON.parse(readFileSync('.data/balance-review-20260908/armor-user-multiplicative-v7.json','utf8'));
const pct=x=>(x*100).toFixed(2)+'%',sg=x=>(x>0?'+':'')+Number(x.toFixed(2))+'%',gain=x=>sg((x-1)*100);
const table=(h,r)=>'| '+h.join(' | ')+' |\n| '+h.map(()=>'---').join(' | ')+' |\n'+r.map(v=>'| '+v.join(' | ')+' |').join('\n');
const effect=s=>[['hit','命中修正'],['evade','闪避修正'],['hp','生命上限'],['mp','魔力上限'],['critAvoid','暴免修正'],['critReduction','暴抗修正'],['reduction','减伤']].filter(([k])=>s[k]).map(([k,v])=>v+'+'+s[k]+'%').join('；');
const rows=[];for(const [name,v]of Object.entries(d.profiles))for(const [slot,w]of [['衣服、裤子，每件',1],['头肩、腰部、脚部，每件',.75]])rows.push([name,slot,...v.map(x=>sg(x*w))]);
const plate=d.full.find(x=>x.name==='板甲'),cloth=d.full[0];
const text=`# 用户甲类单件与套装乘算方案 V7

> 全部成长、装备、甲类、怪物、机巧与迁移内容已合并至[玩家成长与装备战斗数值改动整合方案](玩家成长与装备战斗数值改动整合方案.md)。本文保留为甲类专项复算材料。

日期：2026-09-08。**当前有效口径：用户两张表原值＋单件独立乘算。已数学复算，尚未实装，也未认定已平衡。**

本版合并用户最新单件表、最新3／5件套表，以及“所有单件百分比都是乘算”的明确说明。V5／V6按加算得到的整套-68%、板甲命中24.24%等结果全部撤回，不适用于本方案。V1—V4候选甲类表也不替代本次用户原值。

## 1. 结算方式

每件防具的六项百分比分别作为独立乘区：

~~~text
q_i = 0.6 + 品质_i/250
甲类修正后属性 = 甲类修正前属性 × Π[1 + 单件百分比_i/100 × q_i]
~~~

先用品质确定每件比例，再逐件相乘；先乘后汇总，不先合并比例。中间保留精度，按面板规则在最后取整。示例为100%品质。

- 两件+16%、三件+12%：1.16²×1.12³=1.8904711168，相当于+89.05%。
- 两件-16%、三件-12%：0.84²×0.88³=0.4808466432，相当于-51.92%，保留48.08%。
- 正负件混搭，例如一件+16%和一件-16%：1.16×0.84=0.9744，相当于-2.56%，不会正好抵消。

这个口径确实减轻多个负值累积的扣减、放大多个正值累积的收益；但命中、暴击等仍经过原对抗公式，收益递减不会因此消失。每个单件负值大于-100%时，乘算也不会因累计件数直接跨成负属性。

六维转换、直接双防、普通主词条2:1预算不在本次改动中。单件甲类六项使用乘算；套装仍按件数触发，5件覆盖3件。套装不是每件重复给一份，概率修正也不是直接加相同百分点。

## 2. 用户单件原表

${table(['甲类','部位','命中','闪避','暴免','暴抗','韧性','速度'],rows)}

小部位为衣裤数值的3/4，按用户原值计算，不套用旧版附带属性权重。直接双防仍沿用原先共同预算：衣裤各占整套24%，三个小部位各占17.333…%，五类同条件下相同。

## 3. 用户套装原表

${table(['甲类','3件套','5件套（覆盖3件）'],Object.entries(d.userSets).map(([name,s])=>[name,effect(s[3]),effect(s[5])]))}

板甲3件与5件的暴免／暴抗修正均为12%，按用户原表保留，没有改成更高数值。

~~~text
最终命中=[p+(1-p)×命中修正]×(1-目标闪避修正)
最终暴击=原暴击概率×(1-目标暴免修正)
最终额外暴伤=原额外暴伤×(1-目标暴抗修正)
~~~

必中、强制暴击与减伤80%总上限保留当前规则。没有把逐件乘算误用为3件、5件两档套装叠加。

## 4. 五件乘算后的等效属性变化

${table(['甲类','命中','闪避','暴免','暴抗','韧性','速度'],d.full.map(x=>[x.name,...x.withoutSet.total.map(sg)]))}

这张表用于展示最终等效增减幅，不是回到加算。例如原数值1000，布甲命中约1890.47；板甲命中约480.85、暴免约1890.47，最后再按面板取整。

## 5. 按当前战斗公式重算

统一比较条件：甲类修正前双方对应命中、闪避、暴击与抗性数值均1000；相同直接双防，100%品质，己方穿完整五件，对手没有套装修正、技能命中保底或强制命中。共同双防相同，因此本表隔离甲类差异，不是穿甲与完全裸装的比较。

无套装：

${table(['甲类','命中率','敌方命中率','敌方暴击率','直击指标'],d.full.map(x=>{const f=x.withoutSet;return[x.name,pct(f.hit),pct(f.incoming),pct(f.crit),f.score.toFixed(3)];}))}

加上用户完整五件套：

${table(['甲类','命中率','敌方命中率','敌方暴击率','期望输出增减','有效生命增减','直击指标'],d.full.map(x=>{const f=x.withUserSet;return[x.name,pct(f.hit),pct(f.incoming),pct(f.crit),gain(f.output),gain(f.ehp),f.score.toFixed(3)];}))}

直击指标=相对期望直击输出×相对有效生命，基线1；不是胜率或实际战斗时长。魔力续航、速度先手、控制、技能、神器、机巧均没有折成这个指标。

纠正后的结论：板甲命中为32.47%、敌方命中67.53%，不再是旧加算的24.24%／75.76%；带套装的有效生命现在提高10.29%，不能继续沿用加算稿“有效生命下降”的说法。但布甲命中76.82%、敌方命中23.18%，期望输出+53.64%、有效生命+115.71%，中性直击指标仍约为板甲的${(cloth.withUserSet.score/plate.withUserSet.score).toFixed(2)}倍。

因此，**乘算解决了负值扣除过量的一部分问题，但尚未解决五类甲之间的平衡**。布甲同时得到较大的命中数值、闪避数值、速度乘算，再叠33%／33%概率修正；板甲的防护主要抵消暴击额外部分，仍需承担较高的命中、闪避代价。

## 6. 缺件与品质检查

板甲按衣服、裤子、头肩、腰部、脚部依次穿装：

${table(['件数','套装档位','命中率','敌方命中率','直击指标'],d.partial.find(x=>x.name==='板甲').rows.map(x=>[x.count,x.tier,pct(x.hit),pct(x.incoming),x.score.toFixed(3)]))}

完整板甲的品质对照，固定直接双防、只隔离附带属性与套装：

${table(['品质','命中率','敌方命中率','直击指标'],d.qualityRows.filter(x=>x.name==='板甲').map(x=>[x.quality,pct(x.hit),pct(x.incoming),x.score.toFixed(3)]))}

实际高品质还会提升直接双防，不能凭这张隔离表断言完整配装升级一定变弱；但负向机动项随品质增强的代价依然存在，实施前应合并主词条验收。乘算本身不会自动消除这个问题。

## 7. 建议与验证边界

以用户确认的独立乘算作为后续唯一计算口径，原样保存两张数值表。下一轮平衡优先同时检查布甲概率套装与重／板甲机动代价，而不是继续改变用户的叠加含义。若希望原单件值保持不动，就应单独重做套装预算；不能把当前结果直接标为五类平衡。

本轮不擅自替换任何单件或套装数值。早先按加算反推出的生命补偿建议也已失效。现行数值与模式均记录于本稿，便于按相同条件继续调整。

数学验证包括用户五件乘积、板甲实际概率、混搭概率边界、1—5件、0／60／100品质，以及中性3125种配装枚举。没有重新读取数据库、没有实际技能战斗，也没有将V4的多场景结果作为本版证据。游戏源码与数据库均未修改。

- [审计脚本](../.data/balance-review-20260908/armor-user-multiplicative-v7.ts)
- [完整数值结果](../.data/balance-review-20260908/armor-user-multiplicative-v7.json)
- [文档生成器](../.data/balance-review-20260908/build-armor-user-v7.mjs)
- 源码依据：[combat-math.ts](../src/game/combat-math.ts)、[constants.ts](../src/game/constants.ts)。
`;
writeFileSync('docs/用户甲类单件与套装乘算方案V7.md',text);
console.log(JSON.stringify({document:'docs/用户甲类单件与套装乘算方案V7.md',chars:text.length}));
